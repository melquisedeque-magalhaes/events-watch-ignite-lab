name: "Continuos Integration (CI) Build"

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  Dependesse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Install dependesse
        run: yarn 

      - name: Run Build
        run: yarn build